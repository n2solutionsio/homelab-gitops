# Cilium CNI — replaces flannel, kube-proxy, and MetalLB
# Deployed via Helm in cloud-init (bootstrap), adopted by ArgoCD with ServerSideApply

# Replace kube-proxy with eBPF-based service handling
kubeProxyReplacement: true

# Direct API server access — required when kube-proxy is disabled
# Without this, Cilium tries ClusterIP which needs kube-proxy (chicken-and-egg)
k8sServiceHost: 10.30.30.10
k8sServicePort: 6443

# k3s v1.33 containerd uses standard CNI paths (not k3s-specific)
cni:
  binPath: /opt/cni/bin
  confPath: /etc/cni/net.d

# IPAM — use k3s default pod CIDR
ipam:
  mode: cluster-pool
  operator:
    clusterPoolIPv4PodCIDRList:
      - 10.42.0.0/16

# L2 announcements — replaces MetalLB L2 mode
l2announcements:
  enabled: true

externalIPs:
  enabled: true

# LoadBalancer IP pool managed via CiliumLoadBalancerIPPool CRD in resources/

# Hubble — observability
hubble:
  enabled: true
  relay:
    enabled: true
  ui:
    enabled: true
    ingress:
      enabled: true
      annotations:
        traefik.ingress.kubernetes.io/router.entrypoints: websecure
        traefik.ingress.kubernetes.io/router.tls: "true"
      hosts:
        - hubble.homelab.n2solutions.io

# Operator settings
operator:
  replicas: 1
