# Traefik IngressRoute to expose unifi-mcp-server for Claude Code access.
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: unifi-mcp
  namespace: kagent
spec:
  entryPoints:
    - websecure
  routes:
    - match: "Host(`unifi-mcp.homelab.n2solutions.io`)"
      kind: Rule
      services:
        - name: unifi-mcp-server
          port: 8000
  tls: {}
